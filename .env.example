# ------------------------------------------------------------------
# Brobond AI Commerce OS — Environment Variables
# Copy this file to `.env` and fill in the values.
#
# ENVIRONMENT CONTRACT (PR000.2)
# The runtime reads exactly four variables:
#   AUTH_SECRET · NEXTAUTH_URL · DATABASE_URL · AUTH_TRUST_HOST
# `APP_URL` and `AUTH_URL` were removed — nothing in the runtime used them.
#
# SECURITY: AUTH_SECRET and DATABASE_URL are server-only. They must never be
# exposed to client components (no `NEXT_PUBLIC_` prefix, ever).
# ------------------------------------------------------------------

# --- NextAuth v5 ---------------------------------------------------
# Signing secret. Generate with: openssl rand -base64 32
AUTH_SECRET="replace-me-with-a-long-random-string"
# Canonical deployment URL used by NextAuth for callbacks/redirects.
NEXTAUTH_URL=http://localhost:3000
# Trust the reverse-proxy Host header (required on Render / behind Docker).
AUTH_TRUST_HOST=true

# --- Database (PostgreSQL) ----------------------------------------
# Local docker-compose default. Change for production (Render, etc).
DATABASE_URL="postgresql://brobond:brobond@localhost:5432/brobond?schema=public"

# --- Optional -------------------------------------------------------
# NODE_ENV=development

# --- Local seed bootstrap (development only) ------------------------
# There is NO public sign-up. The seed provisions the first ADMIN account
# for the `brobond` Organization. If SEED_ADMIN_PASSWORD is unset, the seed
# creates the user WITHOUT a password (login disabled) instead of inventing one.
# SEED_ADMIN_EMAIL=admin@brobond.ai
# SEED_ADMIN_PASSWORD=

# ------------------------------------------------------------------
# The keys below are RESERVED for future PRs. They are intentionally
# NOT implemented — only the architecture is prepared.
# ------------------------------------------------------------------

# --- TikTok API (PR002 / PR004) — NOT IMPLEMENTED -----------------
# TIKTOK_CLIENT_KEY=
# TIKTOK_CLIENT_SECRET=
# TIKTOK_REDIRECT_URI=

# --- OpenAI / AI Providers (PR003) — NOT IMPLEMENTED --------------
# OPENAI_API_KEY=
# AI_MODEL=gpt-4o-mini

# --- Analytics Pipeline (PR006) — NOT IMPLEMENTED -----------------
# ANALYTICS_WRITE_KEY=
