/**
 * Instagram connector (PR005) — PLACEHOLDER. No real API is integrated.
 * Resolve it through `getConnector(ConnectorPlatform.INSTAGRAM)`.
 */
export { InstagramConnector } from "./instagram.connector";
